# 验证记录

## 星座内容与最终预览

2026-09-22：补入猎户座、仙后座、大熊座与北斗三份完整档案，连同原有内容共六份。加入星座档案／宇宙基础分类，默认显示猎户座。预览已重新启动在 `http://127.0.0.1:4173/#reading`。

- 生产构建及三项自动测试通过，额外检查所有档案编号唯一、星图线段索引有效。
- 生产预览浏览器验证通过：六个文章地址、刷新、分类切换与记忆、分类内循环、方向键与 Home/End、页签与目录跳转、返回选档与焦点。
- 五种宽度无横向溢出；移动端触控模拟可切档、读取及切换正文；减少动态效果符合预期，无页面运行时错误。
- 桌面与手机最终截图已检查：`test-results/constellation-archive-desktop.png`、`constellation-reader-desktop.png`、`constellation-archive-mobile.png`、`constellation-reader-mobile.png`。
- 星座识别与观测知识依据正文参考资料中的 NASA 页面；示意图不声称精确位置、星等、距离或实时可见性。

## 档案展示改版

2026-09-21：参照 `D:\MyProgram\RhineLabUI` 中的档案浏览截图、详情截图与阅读结构，将三篇现有文章改为实体档案式浏览和分栏阅读。参考项目未修改。

- [x] 每篇文章有稳定档案编号、主题封面、分类和文献依据，原文与原地址保留。
- [x] 切档按钮、编号定位、左右键与首尾循环正常；读取后打开对应文章。
- [x] 概览、正文、参考资料页签与键盘操作正常；目录定位对应正文标题。
- [x] 返回档案库恢复选档与读取入口焦点；直接访问、刷新及浏览器返回正常。
- [x] 档案库和详情在 320、390、768、1024、1440 像素宽度无横向溢出。
- [x] 减少动态效果模式取消选档过渡；未新增三维运行时或依赖。
- [x] 浏览器无页面运行时异常，桌面与手机截图已检查。

本次截图为 `test-results/archive-desktop.png`、`archive-reader-desktop.png`、`archive-mobile.png`、`archive-reader-mobile.png`。

本次验证：2026-09-21。本地 Node.js 22.16.0，Chromium 153，Windows。

## 自动检查

- `npm run build`：通过严格 TypeScript 检查与 Vite 生产构建。
- `npm test`：3 项通过，涵盖 UTC、排序去重、零值、无效输入、空响应、时效边界、AU 换算与文章完整性。
- 浏览器无页面运行时异常。真实 NOAA 接口可从浏览器读取。
- 行星选择框、横向条目联动、鼠标拖拽、暂停、重置视角正常。
- 三个文章地址可直达、刷新、返回；未知文章显示明确返回入口。
- 在浏览器中拦截接口模拟成功、网络失败、空数据、格式错误、过期数据和恢复；各状态符合预期，失败时保留已有观测。
- 320、390、768、1024、1440 像素宽度无横向溢出；移动导航展开、定位及收起正常。
- 减少动态效果时停止自动运动与入场位移；键盘跳过导航入口正常。
- 禁用 WebGL 后二维轨道与行星选择可用，三维操作控件禁用并更换提示。

## 视觉自检

- [x] 无三张等宽居中功能卡片。
- [x] 无紫蓝渐变。
- [x] 无 Emoji 功能图标，使用 Lucide。
- [x] 天文数值有 NASA、IAU 或 NOAA 来源链接，观测时间独立标注；界面刷新与过期阈值注明为本站规则。
- [x] 非对称布局至少两处：Hero 5/7、知识区 8/4；行星区与文章列表也使用主次结构。
- [x] 轨道运动与拖拽、滚动淡入、悬停展开、导航滚动状态变化均已实现；减少动态效果有独立处理。

桌面、手机、知识区、观测区与降级模式截图保存在 `test-results/`，用于本地视觉检查，不进入版本控制。状态测试截图可能包含测试注入的数值，不作为科学观测记录。

## 已知限制

- Three.js 延迟分包约 868 kB（gzip 约 232 kB），Vite 给出体积提示。文章页不主动加载此模块；未进行真实设备 Core Web Vitals 测量。
- 响应式验证使用浏览器视口模拟，未声称完成真机触控验收。
- NOAA 依赖外部网络与服务可用性，页面提供失败/重试状态；没有伪造离线观测数据。
- 原 `astronony` 空目录被工作区占用，Windows 拒绝重命名。成品已建立在目标 `astral-atlas` 路径，原目录未删除。
